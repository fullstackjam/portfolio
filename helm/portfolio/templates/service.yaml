apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
  labels:
    {{- include "portfolio.commonLabels" . | nindent 4 }}
spec:
  selector:
    {{- include "portfolio.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
