# Template values for your application
# Copy this chart and modify these values for your project

# Application configuration
app:
  name: portfolio                    # Change this to your app name (also used as namespace)

# Deployment configuration
replicaCount: 1

# Container image
image:
  repository: fullstackjam/portfolio  # Change this to your image repository
  tag: latest                         # Change this to your image tag

# Ingress configuration
ingress:
  host: portfolio.fullstackjam.com   # Change this to your domain
