replicaCount: 3

image:
  repository: registry.fullstackjam.dev/app/portfolio
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  host:
    - host: portfolio.fullstackjam.dev
    - host: www.fullstackjam.dev
  tlsSecret: portfolio-tls-certificate

imagePullSecrets:
  name: my-harbor-secret
