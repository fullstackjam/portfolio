replicaCount: 3

image:
  repository: registry.fullstackjam.dev/app/portfolio
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: angular-app.yourdomain.com
      tlsSecret: angular-app-tls
