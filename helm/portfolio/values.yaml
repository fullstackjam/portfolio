replicaCount: 1

image:
  repository: registry.fullstackjam.com/portfolio
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: portfolio.fullstackjam.com
  tlsSecret: portfolio-tls-certificate
