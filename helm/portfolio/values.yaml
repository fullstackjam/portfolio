replicaCount: 2

image:
  repository: harbor.fullstackjam.dev/app/portfolio
  pullPolicy: Always
  tag: latest

ingress:
  enabled: true
  hosts:
    - host: portfolio.fullstackjam.dev
  tlsSecret: portfolio-tls-certificate

imagePullSecrets:
  name: my-harbor-secret
